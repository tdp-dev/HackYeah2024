export const shadow = {
    shadowColor: '#000', // Black shadow
    shadowOffset: { width: 0, height: 0 }, // Offset the shadow
    shadowOpacity: 0.1, // Shadow transparency (0 to 1)
    shadowRadius: 0.8, // How blurry the shadow is

    // Android shadow (elevation)
    elevation: 5, // Higher values create larger shadows
};
